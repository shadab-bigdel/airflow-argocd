executor: "KubernetesExecutor"
airflow:
 extraPipPackages:
  - boto
   #- "pymongo==4.6.1" # https://pymongo.readthedocs.io/en/stable/changelog.html 4.8.0
   #- "pandasdmx==1.10.0" # https://pypi.org/project/pandaSDMX/ 1.10.0
   #- "pandas==2.2.2"
   #- "openpyxl==3.1.2"
  #- "apache-airflow-providers-amazon==8.2.0"  #apache-airflow-providers-amazon==8.7.1
dags:
  persistence:
    # Annotations for dags PVC
    annotations: {}
    # Enable persistent volume for storing dags
    enabled: false
    # Volume size for dags
    size: 1Gi
    # If using a custom storageClass, pass name here
    storageClassName:
    # access mode of the persistent volume
    accessMode: ReadWriteOnce
    ## the name of an existing PVC to use
    existingClaim:
    ## optional subpath for dag volume mount
    subPath: ~
  # GitSync configuration
  gitSync:
    enabled: true
    # Git repository URL
    repo: https://github.com/shadab-bigdel/airflow-helm-chart.git
    branch: main
    # The Git revision to check out (branch, tag, or hash)
    ref: main
    depth: 1
    # Maximum number of consecutive failures allowed before aborting
    maxFailures: 0
    # Subpath within the repo where DAGs are located
    # Should be "" if DAGs are at the repo root
    subPath: "dags"